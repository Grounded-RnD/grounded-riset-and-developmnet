import React from 'react'

interface Proops {
    children: React.ReactNode
}

export default function ModalProfile(children: Proops) {
  return (
    <React.Fragment>
        
    </React.Fragment>
  )
}

